---
  - hosts: localhost
    gather_facts: false
    vars:
      tag: "{{ Server_name }}"
    roles:
      - ec2-instance
    debug:
      "{{ tag }}"  
...      
  #- import_playbook: "{{ role }}.yml"