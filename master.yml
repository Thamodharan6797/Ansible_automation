---
  - hosts: localhost
    gather_facts: false
    vars:
      servername: demoserver
    #roles:
    #  - ec2-instance
    
    tasks:
      - name: Check the value of server name 
        debug: 
          msg: "{{ servername }}"
        
...      
  #- import_playbook: "{{ role }}.yml"