---
  - hosts: localhost
    gather_facts: false
    vars:
      tag: "{{ Server_name }}"
    #roles:
    #  - ec2-instance
    
    tasks:
      - name: Check the value of server name 
        debug:
          "{{ tag }}" 
...      
  #- import_playbook: "{{ role }}.yml"