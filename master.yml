---
  - hosts: localhost
    gather_facts: false
    vars:
      servername: "{{ server_name }}"
    #roles:
    #  - ec2-instance
    
    tasks:
      - name: Check the value of server name 
        debug:
          "{{ servername }}"
...      
  #- import_playbook: "{{ role }}.yml"